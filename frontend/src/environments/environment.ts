export const environment = {
  production: false,
  // Use localhost when running the frontend on the host machine (ng serve / dev).
  // In Docker production build the frontend container should use the service name (backend).
  apiUrl: 'http://127.0.0.1:5000'
};
